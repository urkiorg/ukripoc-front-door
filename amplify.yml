version: 0.1
backend:
    phases:
        build:
            commands:
                - npm install -g @aws-amplify/cli@latest
                - npm run init-lambda
                - amplifyPush --simple
frontend:
    phases:
        preBuild:
            commands:
                - yarn install
        build:
            commands:
                - yarn build
    artifacts:
        baseDirectory: build
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
